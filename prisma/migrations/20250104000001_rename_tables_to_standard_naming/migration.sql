-- Rename tables to use standard naming convention (plural, lowercase)

-- Rename Project to projects
RENAME TABLE `Project` TO `projects`;

-- Rename Experience to experiences  
RENAME TABLE `Experience` TO `experiences`;

-- Rename Tech to tech
RENAME TABLE `Tech` TO `tech`;

-- Rename Media to media
RENAME TABLE `Media` TO `media`;

-- Rename Link to links
RENAME TABLE `Link` TO `links`;

-- Rename Training to trainings
RENAME TABLE `Training` TO `trainings`;
